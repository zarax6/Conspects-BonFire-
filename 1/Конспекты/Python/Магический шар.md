```python
import random 
answer = ['Да', 'Нет', 'В раздумиях']
while True:
	x = input("Ваш вопрос? Введите leave для выхода.\n")
	if x.find("leave") != -1:
		print("Пока пользователь.")
		break
	else:
		print(random.choice(answer))
``` 