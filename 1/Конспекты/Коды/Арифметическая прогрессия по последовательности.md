``` python
class Solution:
    def canMakeArithmeticProgression(self, arr: List[int]) -> bool:
        arr = sorted(arr)   # сортировка исходного массива
        arr_len = len(arr)   # длина массива arr
        diff = arr[0] - arr[1]   # разница между первым и вторым элементом отсортированного списка
        diff2 = 0

        if arr_len == 2:   # проверка, если последовательность содержит 2 символа
            return True

        for i in range(1, arr_len):
            if i + 1 != arr_len:
                diff2 = arr[i] - arr[i + 1]
                print(diff2)
            if diff != diff2:
                return False
        return True
```

В данном коде мы пробуем получить прогрессию из неотсортированного массива.

#### Сортировка
Мы сортируем наш массив от меньшего к большему если это возможно

####  Длина
Определяем длину массива что бы не вызывать ее много раз

####  Разница
Высчитываем разницу между двумя элементами отсортированного списка

#### Проверки
1. Проверяем если последовательность содержи 2 символа ( их не надо высчитывать )
2. Проверяем длину нашего отсортированного массива и ищем разницу
3. Проверка на разницу