![[Pasted image 20260114120118.png]]
```TypeScript
function lengthOfLastWord(s: string): number {
    s = s.trim();
    let lst_space: number = s.lastIndexOf(" ");
    let lst_word: string = s.slice(lst_space + 1);
    console.log(lst_word)
    return lst_word.length;
};
```
